/target
**/*.rs.bk

# Claude Code
.claude/*.local.*
CLAUDE.local.md
.claude/worktrees/

# Agent scratch: plans, specs, task briefs and reports.
.tmp/
.superpowers/

# Vagrant's own per-machine state (box/box, provider config, SSH/WinRM keys) - devvm.py
# guests are throwaway; this directory is regenerated by every `devvm.py up`.
.vagrant/
